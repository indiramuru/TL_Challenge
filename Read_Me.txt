-Run 'index.html' file via any server to avoid CROS errors in chrome and see the output across any browser.
-'data.json' is having some simple form of json data. 
-HTML, CSS and JS code - all are in 'index.html'. 
-Application searches on 'actor' and 'director';
   -Changing array value "$scope.eType = ['actor','director'];" in "project" controller would search across the added category.

